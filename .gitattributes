# Shell scripts must use LF line endings or the shebang line
# breaks on macOS / Linux ("bad interpreter: /bin/bash\r").
*.sh         text eol=lf
*.command    text eol=lf
*.bash       text eol=lf

# Windows batch files must stay CRLF.
*.bat        text eol=crlf
*.cmd        text eol=crlf
