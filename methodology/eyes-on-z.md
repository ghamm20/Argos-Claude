# Eyes-on Verification — Z Phases (Demo Quality-of-Life)

**Status:** **PASS** — five small chat-surface improvements landed, all green on `npm run check`, all pushed to GitHub with CI passing.

**Verifier:** Claude on Win11 + i7-11700F + RTX 3060 Ti
**When:** 2026-05-21
**Branch:** `main`
**Trigger:** operator instruction "lets keep going please" / "lets start tackling them" after the H8.5 autonomous block landed.

The Z phases are scope-bounded chat-surface polish that respects the original doctrine (no host writes, no new server features past v1 scope-lock, no UI bloat). Each phase ships with smoke coverage or self-evident verification, and each was pushed to GitHub independently — making it easy to revert any single phase if it breaks something.

## Phase Z1 — Chat export to markdown + clear-chat button

**Files:** `lib/chat-export.ts` (new, pure), `components/ChatPane.tsx`, `scripts/smoke-chat-export.mjs` (new).

**Surface change:**
- Two icon buttons in the top-right of the chat scroller, visible only when `messages.length > 0`
- Download icon → blob URL → `argos-chat-YYYYMMDD-HHMM-{persona-slug}.md`
- Trash icon → `window.confirm` → `clearChat()` from store
- Trash button disabled while `isStreaming`

**Markdown format:**
```markdown
# ARGOS Chat — Bartimaeus

**Exported:** 2026-05-21T22:25:00.000Z
**Model:** llama3.1:8b-instruct-q4_K_M
**Messages:** 4

---

### User
What does Rule 3 say?

### Bartimaeus
Per Rule 3 [1], paths must derive from ARGOS_ROOT.

**Citations:**

- **[1]** `seven-rules.md` chunk 2 (score 0.598) — Rule 3 — Relative paths only.

---

_Generated by ARGOS — local-first AI workstation._
```

System messages are skipped (framing, not conversational). Empty content renders `*(empty)*` to preserve turn structure.

**Verification:** `scripts/smoke-chat-export.mjs` — 27/27 PASS across 7 scenarios (empty chat, basic user+assistant, system-skip, citations rendered, long-citation truncation, empty-content fallback, filename slug).

**Doctrine compliance:** zero host writes from the app. The browser saves the .md to the operator's chosen download dir — that's their write, not the application's.

**Commit:** `09ead2a`

## Phase Z2 — "thinking…" pre-first-token indicator

**Files:** `components/ChatPane.tsx`

**Problem solved:** during the 5–10s cold model load on the first chat of a session, the assistant bubble previously showed an empty content area + a blinking caret. That visually identical to a frozen app. Operator demo risk: looks like crash.

**Solution:** when `{isStreaming: true, content === ""}`, render an inline italic "thinking…" with a pulsing dot in the persona's accent color. Once the first token arrives, the normal content + caret render resumes.

**Verification:** eyes-on visible. No automated smoke (visual state — would need browser test).

**Doctrine compliance:** no new external dependencies; reuses existing framer-motion.

## Phase Z3 — Stop streaming

**Files:** `components/ChatPane.tsx`

**Demo-critical feature:** if a chat goes off the rails (LLM hallucinating, generating endlessly, model loaded wrong), the operator needs a way to halt without page refresh.

**Implementation:**
- `useRef<AbortController | null>(null)` per send invocation
- `controller.signal` threaded through `fetch("/api/chat", { signal: ... })`
- Send button swaps to a red Stop button while `isStreaming`
- Stop button → `controller.abort()` → fetch throws AbortError → caught and treated as a clean stop, not an error
- Transcript shows `_[stopped by operator]_` appended to whatever content streamed before abort
- `isStreaming` cleared, HUD metrics preserved for the partial response

**Server-side:** the chat route already had `AbortController` wiring for the upstream Ollama fetch. The client abort propagates via `reader.cancel(reason)` in the stream's `cancel` callback.

**Verification:** eyes-on (no automated browser test). Server-side abort wiring confirmed in `app/api/chat/route.ts` lines 242–246.

**Commit:** `49c9487` (combined Z2+Z3)

## Phase Z4 — `npm run warm` model preload

**Files:** `scripts/warm-ollama.mjs` (new), `package.json`, `docs/05-OPERATIONS.md`

**Problem solved:** Phase K measured 9.5s real launcher cold-start, of which 6.8s was llama3.1:8b model load. That happens on the FIRST chat of a session. Operator demo risk: "I'm sending a hello message, why is it 8 seconds…"

**Solution:** standalone CLI that pre-loads both models via:
- `POST /api/generate` with `{ model, prompt: "hi", stream: false, options: { num_predict: 1 } }` → returns when model is fully in VRAM
- `POST /api/embeddings` with `{ model, prompt: "warm" }` → returns when embed model is loaded

After running, first user-visible chat hits at sub-second TTFB.

**Behavior:**
- Reads `OLLAMA_HOST` env (matches launcher + `lib/ollama-config.ts`)
- Tolerant model-name lookup (`llama3.1:8b...` vs `name:latest` matching)
- 30s daemon-availability wait at startup
- 120s timeout on chat warm (covers slowest cold-load on weak hardware)
- Exits non-zero with actionable `ollama pull X` hint if a model is missing

**Verification:** ran against live host daemon:
```
[1/4] waiting for ollama daemon ... ready (34ms)
[2/4] listing installed models ... 4 found
[3/4] warming chat model (llama3.1:8b-instruct-q4_K_M) ... ok (7492ms, load_duration=7443ms)
[4/4] warming embed model (nomic-embed-text:latest) ... ok (1852ms, 768 dims)
```

Total: 9.3s wall time. Models then warm.

**Operator flow:**
```
Terminal 1: launcher.bat   (10s to ready)
Terminal 2: npm run warm   (10s to warm)
→ First chat: sub-second TTFB
```

**Commit:** `5021e2d`

## Phase Z5 — Keyboard shortcuts

**Files:** `components/ChatPane.tsx`

**Shortcuts added:**
- `Esc` (no modifier) → stop streaming if `isStreaming === true`
- `Cmd/Ctrl+K` → clear chat (same `window.confirm` guard as the button)
- `Cmd/Ctrl+E` → export chat as markdown

**Guard:** ignores keystrokes when the user is typing in:
- Any other `INPUT` or `SELECT`
- Any other `TEXTAREA` (the chat textarea itself is exempt to avoid breaking its own Cmd+Enter for send)
- Any `contentEditable` element

Prevents the operator from accidentally losing draft text or triggering the confirm dialog.

**Implementation:** window-level `keydown` listener inside `useEffect`, cleanly removed on unmount.

**Button titles updated** to surface the shortcuts on hover: "Export chat as markdown (Cmd/Ctrl+E)" and "Clear chat (Cmd/Ctrl+K)".

**Commit:** `28b3901`

## Summary

| Phase | Commit | Surface | Type |
|---|---|---|---|
| Z1 | `09ead2a` | Chat: export to markdown + clear chat | Feature |
| Z2 | `49c9487` | Chat: "thinking…" cold-load indicator | UX |
| Z3 | `49c9487` | Chat: Stop streaming button | Feature |
| Z4 | `5021e2d` | CLI: `npm run warm` | Infrastructure |
| Z5 | `28b3901` | Chat: keyboard shortcuts | UX |
| docs | `30b8cd7` | CHANGELOG.md | Project hygiene |

**All 7 verify-argos rules PASS across all 5 phases.**
**CI green on every push to main.**
**No new runtime dependencies introduced (reused lucide-react, framer-motion, zustand).**

## What's NOT in this block (deferred to bigger swings)

- **Server-side chat history persistence** — filed as Phase Z9; touches store, new API route, new storage primitives. Bigger scope than the Z phases.
- **Memory page as session list viewer** — filed as Phase Z10, blocked on Z9.
- **`scripts/demo-prep.mjs`** — filed as Phase Z8, operator-facing pre-demo wrapper.
- **`npm run demo-check`** — filed as Phase Z7, single-command pre-demo sanity.

These are the next round.
